
# about
This document covers guidelines for implementing the design.

## Spacing
- When spacing between two elements, favor using `margin-top` over `margin-bottom`, and `margin-left` over `margin-right`.
- Don't use logical margin properties.
