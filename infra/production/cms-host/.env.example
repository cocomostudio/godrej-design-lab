#
# The CMS host — the deployment facts for this one machine.
#
# Copy to `.env` beside this file and fill in. `.env` is gitignored; this
# example is the only record of which keys the host needs.
#
# Read by infra/production/cms-host/pm2/ecosystem.config.cjs when PM2 starts
# the app.
#
# This file does NOT configure Strapi. The application reads apps/cms/.env.
#

# Where PM2 writes the process logs. Absolute paths: PM2 resolves a relative
# one against the app's cwd, which is the checkout, and logs do not belong
# there.
CMS_ERROR_LOG=/var/log/gdl/cms.pm2.error.log
CMS_OUT_LOG=/var/log/gdl/cms.pm2.out.log
